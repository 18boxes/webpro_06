<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>教授</title>
</head>
<body>
  <h2>教授一覧</h2>
  <a href="/">トップに戻る</a>
  <% for (let i = 0; i < data.length; i++) { %>
    <p>
      ID：<%= data[i].id %><br>
      名前：<span style="font-size: 28px;"><%= data[i].name %></span><br>
      読み：<%= data[i].yomi %><br>
      年齢：<%= data[i].old %><br>

      <a href="/prof/detail/<%= i %>">［詳細］</a>
      <a href="/prof/delete/<%= i %>"
         onclick="return confirm('本当に削除しますか？');">
         ［削除］
      </a>
    </p>
    <hr>
  <% } %>

  <p><a href="/prof/create">＋ を追加</a></p>
</body>
</html>